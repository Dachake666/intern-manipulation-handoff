# 更新任务与完成标准

本表区分“本轮已经更新的记录”与“需要后续修改的运行代码”。运行改动均应保留冻结原件、在开发版本实现并通过对应检查；不直接修改收到的源码或历史日志。

## 本轮已完成

| ID | 内容 | 完成证据 |
|---|---|---|
| D1 | 将截断包缺件状态更新为完整收件 | receipt / 全量 CRC 与 SHA；9/14 18项收齐 |
| D2 | 从原始 JSON 复算 MPC 十轮 | mpc_review.json；34.4528046065% |
| D3 | 区分 Mac 仿真、Debian 仿真、Shadow、Active | MPC 分项说明与报告平台/核心哈希 |
| D4 | 记录 Servo 61 份与 MPC 13 份结果的不同意义 | servo_review / mpc_review；早期 Shadow 失效仍完成基础运动单列 |
| D5 | 保存源码、备份映射与脱敏日志副本 | preservation_manifest / received_sources.zip / logs |
| D6 | 标注同名异义、历史脚本与源包漂移 | source_comparison / FILE_USE_GUIDE |
| D7 | 核对手眼服务与原始配对数据 | vision_review / handeye_data_manifest |
| D8 | 更新总入口并形成中文归档提交 | START_HERE 新段；提交身份以 Git log 为准 |

## P0：下一轮开发优先处理

| ID | 问题 | 处理方式 | 完成标准 / 负责人 |
|---|---|---|---|
| U1 | 手眼高残差设置 -105 后仍写配置，随后错误被清零 | 质量失败立即拒绝保存有效外参；保留失败结果供诊断 | 软件修复及失败分支测试通过；用户确认实际部署工程/启动配置后再部署 |
| U2 | Servo/MPC wrapper 删除失败 JSON，异常路径缺 MPC 状态 | 成功与失败都输出脱敏结构化记录，finally 保存配置、依赖、错误与收尾 | 求解失败、SDK失败、软停、首点失败的离线假 SDK 测试都留下可追踪报告 |
| U3 | MPC core / VAJ3 spline 不在历史运行哈希表中 | 新日志记录所有动态导入模块真实路径、完整SHA和依赖版本 | 新运行可从报告重建实际文件组合；不追补旧记录为当时已绑定 |
| U4 | Debian 迟到继续、软停每10帧与 Mac 策略不同 | 明确响应目标，将预构造性能优化与停止策略分别评审 | 迟到、软停、连续失败等测试能证明预期停止/收尾；不直接整目录覆盖 |
| U5 | GUI 现场报告存在历史重绑 | 保留旧原件，后续修改后的实际指令另做完整人工 GUI 和资格检查 | 新轨迹SHA与真实审核一致，不把文件收齐当新审核通过 |

## P1：工程集成和后续验证

| ID | 问题 | 下一步 | 完成标准 / 前置 |
|---|---|---|---|
| U6 | Active 后处理改变QP输出，动态约束未完整复验 | 对最终发出的渐收指令复验步长、速度、加速度；评估持物与全臂净空 | 最终命令流的离线报告，不只报告原始QP残差 |
| U7 | alpha=0.4和40ms反馈新鲜度未辨识 | 记录设备时间戳/序列号与完整循环耗时，安排受控辨识 | 现场数据支持模型与时序结论；不以QP耗时替代端到端时序 |
| U8 | Debian MPC内calib_common是兼容层 | 统一通过显式模型/配置接口引用Mac共享真源，移除平台绝对路径依赖 | Linux/Mac离线复跑可用；CONFIRMED_*不被简化层覆盖 |
| U9 | 视觉三份手眼源码与多份配置并存 | 确认实际launch、二进制、参数文件及子仓库工作树，再选一份真源 | 确认8个HEAD对应工作树改动；启动配置SHA进入诊断 |
| U10 | 手眼会话清理语义不完整 | request3仅撤销末项；另定义显式会话重置，错误路径行为一致 | 更换workspace_id/失败/重试不会隐式混入旧内存样本；原始文件留存 |
| U11 | 39组样本为四个编号组，旧外参残差高 | 明确每组采集状态、单位、TCP、变换方向，再选合法成对样本复算 | 留出/独立点验证通过；不能只换四元数重配旧图像 |
| U12 | C++视觉输出与Mac任务契约不同 | 接一层显式适配，记录sdk_world、mm、xyzw与OBJECT/GRASP/EE语义 | 回放/虚拟目标先通过任务、规划和无运动检查 |
| U13 | 旧右臂/瓶子执行器放开默认运动或物理限位保护 | 历史版本只作证据；要复用时移植到共享配置和现行安全链 | 控制器实限、工具/TCP和场景通过对应检查后才有人确认上机 |

## P2：文档、依赖和发布收敛

- 修正视觉 README 的 `json_file_name` 为真实接口 `save_json_file`，结果矩阵按实际16元素记录；列出真正的启动路径，避免旧路径误导。
- MPC README 分列四种运行性质；历史离线报告保留原生成源码SHA，不修改为新core SHA。
- 更新 releases 总览与生成器的当前状态映射；运行包只能由 `releases/make_release.py` 生成，不手改副本，不因收齐文件提升 REALVERIFIED。
- Linux 两套虚拟环境、ROS生成物和第三方Git对象不进入精简交接；记录依赖版本后重建。`py_srvcli`等包外/缺失源必须另列。
- 早期MPC三份报告缺精确执行器字节、瓶子五连成功缺完整原日志，这些属于历史缺口：有原件就补，没有就明确保留有限证据，不能用新跑覆盖旧事实。

## 用户只需补充的现场事实

1. 实际运行的是哪个视觉目录、launch命令、配置文件和二进制；当前机器人/相机/工具及桌子是否变化。
2. 若保留着：早期MPC三轮的当时执行器、被删除失败结果对应终端日志、瓶子五轮完整日志。
3. 下一轮优先修Servo/MPC、恢复手眼闭环，还是推进双臂。上面的文件清点和记录不再需要重复打包。

本次未执行推送、部署、真机使能、清故障或运动。Git提交只固化记录，不代表批准运行收到的现场代码。
