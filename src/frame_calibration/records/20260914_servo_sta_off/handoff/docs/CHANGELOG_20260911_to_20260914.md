# 9/11 → 9/14 必须同步的增量

## 从“待测试”变为“已收到实测摘要”

旧 MD 停在短夹爪等待后 candidate/review allowlist 调整阶段。9/11 17:18–17:27 已有三种模式多次完成记录，但网络状态未连续核验；9/14 又收到了 10:14、10:15、10:17、10:57 四次真实轨迹执行输出和关键报告字段。

## 从 ifconfig 临时 down 变为 UCI 禁用

随后查得 `wlan-sta0` 又处于 UP 并关联上游，简单 down 并未可靠保持。新操作针对 `wireless.sta.disabled='1'`、`wifi reload`；确认 STA 消失而 Robot AP 仍工作。未见 commit 与最终恢复证据。

## 最新数字

2780 Pulse，加权均值 4.006274722ms，最大 55.311642122ms；>20ms 21次，>40ms 1次，四轮 realigns 都是0。不能把这批数据说成硬实时或无限次无秒级卡顿。

## 文件和审核

短等待 candidate 新 SHA 为 `71bc5148…a1db338`；本次可从旧原件精确重建。新 GUI review 历史 SHA 为 `02f94490…154b13`，但只是现场重绑后的记录，未建立独立新回放审核。

最新三 field 源码、spline、共用依赖与原 JSON 未回收，添加只读收集工具和缺口矩阵。没有为 Mac 重写未见原件的执行器。

## 行为/结论修订

保留全部三模式；效率导向暂选 Plain20，不删除 Plain25。不能再把 9/11 混杂网络条件中的表现当成算法排序。记录互联网影响是“取决于真正上游”，不能承诺其他联网用户不受影响。完整恢复列表补上此前 Ethernet profile 改动，powersave 原值仍未知。
