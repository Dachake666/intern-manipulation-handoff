# 20260804 Pulse Servo 双抓放 —— 5 轮真机全过（验收基线）

**这是 Track C pulse 第一次跑通「两组 pick-place」完整任务。**

轨迹 `traj_multi_latest.json`，SHA-256 `ac5c6fd7715c81fb...4968`
（与仓库文件逐字节一致，已核对）。133 个关节路点，4 次夹爪事件
（close/open/close/open），左臂 `arm_id=1`。

## 五轮结果

| # | 步长 | 全局速度 | 密集点 | 时长 | 迟到帧 | 最大超时 | 终点误差 |
|---|---|---|---|---|---|---|---|
| 1 | 0.4° | 10% | 1633 | 32.66s | 95 (5.8%) | 106.1ms | 0.048° |
| 2 | 0.2° | 15% | 3232 | 64.64s | 197 (6.1%) | 96.4ms | 0.047° |
| 3 | 0.2° | 15% | 3232 | 64.69s | 46 (1.4%) | 49.7ms | 0.048° |
| 4 | 0.4° | 15% | 1633 | 32.66s | 33 (2.0%) | 190.1ms | 0.046° |
| **5** | **0.4°** | **15%** | **1633** | **32.66s** | **23 (1.4%)** | **21.4ms** | **0.048°** |

五轮均：133 路点限位预检通过 → 四次夹爪前确认停稳 → 完整两组抓放 →
保护恢复 True → 全局速度还原 → SDK 正常停止。**无异常退出。**

## 推荐基线：`0.4° / 20ms`

```bash
python3 execute_servo_grasp.py traj_multi_latest.json --step-deg 0.4 --speed 15
```

相比 `0.2°`，**时间减半（64.6s → 32.7s），终点精度没有下降**。

⚠ **不能说「15% 比 10% 好」**——两者时长同为 32.66s。透传模式下速度由
`STEP_DEG / PERIOD_S` 决定，`armSetGlobalSpeed` 基本不起作用。第 1 轮与第 4/5 轮
的迟到帧差异更可能是网络抖动，不是全局速度的功劳。

## 还没解决的

第 4 轮出现过一次 **190.1ms** 单帧尖峰。没有造成总时长漂移或精度下降，属于
**偶发节拍尖峰**，但值得注意：真机上跑的 `servo_common.py` 当时还是旧版，
**没有「放弃追赶并重新对齐」的保护**——那 190ms 之后的积压帧是被背靠背补发的。
新版已加此保护（落后超 2 个周期就重新对齐），下次跑会打印出来。

## 轨迹本身的优化空间（V2 待做）

**① HOME↔READY 往返占了 53.6% 的时间**

| 段 | 帧数 | 时间 | 占比 |
|---|---|---|---|
| HOME→READY | 401 | 8.02s | 24.6% |
| READY→HOME | 474 | 9.48s | 29.0% |
| 抓放升降合计 | 502 | 10.04s | 30.7% |
| 悬停点间转运 | 256 | 5.12s | 15.7% |

两段起终点相同、都是空爪，但 READY→HOME 比反向多 **73 帧**。
先试直接复用反向路径可省约 1.46s，再用多随机种子规划挑「时间短 + 关节余量大 +
转角小」的候选。

**② 9 处方向突变 >60°**（透传不会停，但速度方向突变会有视觉折动）

```
115.4°  @ PLACE2_HOVER→READY
105.3°  @ PICK1_ASCEND
 96.2°  @ PLACE1_ASCEND
 92.3°  @ HOME→READY
 92.2°  @ PICK2_ASCEND
```

**③ 关节余量偏紧**：`j6 = -32.98°`，距控制器真值下限 -40° 只剩 **7.02°**，
按收 5° 的判定只剩 **2.02°**。规划代价函数应加入「最大化限位余量」。

**不值得做的**：JSON 里 4 条零距离重复边，删掉只省 4 帧（0.08s）；
133 个路点也不必激进抽稀——透传下原始点不造成停顿，抽稀反而让拐角变尖。

## 复现

脚本参数现在走命令行（此前是现场手改常量，仓库版复现不出）：

```bash
python3 execute_servo_grasp.py traj_multi_latest.json \
    --step-deg 0.4 --period-ms 20 --speed 15
```

运行时会打印完整配置和脚本 SHA，归档时对得上。
